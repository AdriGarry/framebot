#!/bin/bash

#
# DEPRECATED, use coreStartup.service instead
#

case "$1" in

    start)
    
        sudo python /home/odi/core/src/python/buttons.py 2>&1 | sudo tee -a /home/odi/core/log/button.py.log &

        sudo sh /home/odi/core/core odi &

    ;;

    stop)

        # sudo killall omxplayer.bin
        # sudo killall node
        # sudo sh /home/odi/core/src/shell/sounds.sh reboot

    ;;
esac

exit 0

