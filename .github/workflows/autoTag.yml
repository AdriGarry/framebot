name: Auto tag

# on:
#   push:
#     branches:
#       - master

on:
  repository_dispatch:
    types: [trigger-workflow-2]

jobs:
  auto-tag-test-yml:
    runs-on: ubuntu-latest
    steps:
    - name: just echo a message 2
      run: |
        echo Hello there, this job should create a git tag!

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: butlerlogic/action-autotag@stable
#         with:
#           GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
#           tag_prefix: 'v'
